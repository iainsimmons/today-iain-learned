---
// MermaidDiagram.astro
// Component for rendering Mermaid diagrams with proper styling and error handling

export interface Props {
  id: string;
  source: string;
  class?: string;
}

const { id, source, class: className = '' } = Astro.props;
---

<div class={`mermaid-diagram ${className}`} data-mermaid-id={id} data-mermaid-source={source}>
  <div class="mermaid-diagram-content">
    <pre class="mermaid-diagram-source"><code>{source}</code></pre>
  </div>
</div>

<style>
  .mermaid-diagram {
    margin: 1.5rem auto;
    max-width: 100%;
    overflow: hidden;
  }

  .mermaid-diagram-content {
    position: relative;
  }

  .mermaid-diagram-source {
    display: none;
  }

  /* Mermaid diagram styling */
  .mermaid-diagram :global(.mermaid) {
    text-align: center;
  }

  /* Error state styling */
  .mermaid-diagram.error {
    border: 1px solid rgb(254, 202, 202);
    border-radius: 0.5rem;
    padding: 1rem;
    background-color: rgb(254, 242, 242);
  }

  :global(.dark) .mermaid-diagram.error {
    border-color: rgb(127, 29, 29);
    background-color: rgba(127, 29, 29, 0.2);
  }

  .mermaid-diagram.error .mermaid-diagram-content {
    color: rgb(185, 28, 28);
  }

  :global(.dark) .mermaid-diagram.error .mermaid-diagram-content {
    color: rgb(252, 165, 165);
  }

  /* Loading state styling */
  .mermaid-diagram.loading {
    border: 1px solid rgb(var(--color-primary-200));
    border-radius: 0.5rem;
    padding: 1rem;
    background-color: rgb(var(--color-primary-50));
  }

  :global(.dark) .mermaid-diagram.loading {
    border-color: rgb(var(--color-primary-600));
    background-color: rgba(var(--color-primary-800), 0.2);
  }

  .mermaid-diagram.loading .mermaid-diagram-content {
    color: rgb(var(--color-primary-600));
  }

  :global(.dark) .mermaid-diagram.loading .mermaid-diagram-content {
    color: rgb(var(--color-primary-400));
  }

  /* Loading skeleton styling */
  .mermaid-loading-skeleton {
    position: relative;
    overflow: hidden;
  }

  .mermaid-loading-skeleton::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(var(--color-primary-200), 0.5), transparent);
    animation: shimmer 2s infinite;
  }

  :global(.dark) .mermaid-loading-skeleton::before {
    background: linear-gradient(90deg, transparent, rgba(var(--color-primary-700), 0.5), transparent);
  }

  @keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
  }

  /* Smooth transitions for rendered diagrams */
  .mermaid-diagram svg {
    transition: opacity 0.3s ease-in-out;
    opacity: 0;
    animation: fadeIn 0.3s ease-in-out forwards;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

/* NUCLEAR BOMB HIDING - Hide all Mermaid error bombs permanently */
.mermaid-diagram svg[aria-roledescription="error"],
.mermaid-diagram svg:has(.error-icon),
.mermaid-diagram svg:has(.error-text),
.mermaid-diagram svg[viewBox="0 0 2412 512"],
.mermaid-diagram svg[viewBox="0 0 417 625"] {
  display: none !important;
  visibility: hidden !important;
  opacity: 0 !important;
  height: 0 !important;
  width: 0 !important;
  overflow: hidden !important;
}

  /* Performance optimizations */
  .mermaid-diagram {
    contain: layout style paint;
  }

  .mermaid-diagram svg {
    will-change: auto;
  }

  /* Responsive design */
  @media (max-width: 768px) {
    .mermaid-diagram {
      margin-left: 0;
      margin-right: 0;
    }
    
    .mermaid-diagram svg {
      max-width: 100%;
      height: auto;
    }
  }
</style>
